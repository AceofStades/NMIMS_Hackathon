To run

python -m venv env
. env/bin/activate
pip install -r requirements.txt

cd django...
python manage.py runserver

If prompted credentials on login screen, use:
username = 'Aryan'
password = '1234'
